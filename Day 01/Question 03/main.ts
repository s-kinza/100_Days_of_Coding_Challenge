// creat a variable name
let myName: string = "Kinza";

// print a name in small letter (toLowerCase)
console.log(myName.toLowerCase());

// print a name in ALL BIG LETTER (toUpperCase)
console.log(myName.toUpperCase());

// print the name with the first letter of big
console.log(myName.charAt(0).toUpperCase() + myName.slice(1).toLowerCase());